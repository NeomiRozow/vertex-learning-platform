# Standalone Studio runs on Vite, so only SANITY_STUDIO_-prefixed vars reach it.
# Use the same values as NEXT_PUBLIC_SANITY_* in the web app's .env.local.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=
